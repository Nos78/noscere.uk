---
// src/components/Bullet.astro
const { class: className, ...rest } = Astro.props;
---

<span
  class:list={["inline-block h-2 w-2 rounded-full bg-cyan-400", className]}
  {...rest}></span><slot />
